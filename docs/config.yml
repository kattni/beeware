# yaml-language-server: $schema=https://squidfunk.github.io/mkdocs-material/schema.json

not_in_nav: |
  /index.md

validation:
  omitted_files: info
  absolute_links: relative_to_docs
  anchors: warn
  unrecognized_links: warn

theme:
  name: material
  language: en
  features:
    - content.tabs.link
    - toc.follow
    - content.code.copy  # Ask about fixing this
    - navigation.indexes
    - navigation.expand
  palette:
    # Palette toggle for light mode
    - scheme: default
      primary: deep purple
      toggle:
       icon: material/brightness-7
       name: Switch to dark mode

    # Palette toggle for dark mode
    - scheme: slate
      primary: deep purple
      toggle:
       icon: material/brightness-4
       name: Switch to light mode

markdown_extensions:
  pymdownx.highlight:
    anchor_linenums: true
  pymdownx.superfences: {}
  pymdownx.blocks.admonition: {}
  pymdownx.caret: {}
  pymdownx.blocks.tab:
    alternate_style: true
  toc:
    permalink: true
    title: On this page
  attr_list: {}

extra:
  "beta_support": ^&beta;^
  "not_supported": ^&#x274C;^

plugins:
  search: {}
  autorefs: {}
  include-markdown: {}
  literate-nav:
    nav_file: SUMMARY.md
  macros: {}
  table-reader: {}
  spellcheck:
    backends:
      - codespell:
          dictionaries: [clear]
    # Location of spelling_wordlist.txt is relative to docs_dir in mkdocs.x.yml.
    # This is the location of it relative to the temp directory for translations.
    known_words: ../spelling_wordlist
    ignore_code: yes
